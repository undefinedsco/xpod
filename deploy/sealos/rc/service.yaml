apiVersion: v1
kind: Service
metadata:
  name: xpod-rc
  namespace: xpod-rc
  labels:
    app: xpod-rc
spec:
  selector:
    app: xpod-rc
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  type: ClusterIP
