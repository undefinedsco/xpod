# === Xpod Configuration Template ===
# See docs/architecture-v2.md for detailed documentation

# === XPOD 配置 ===
XPOD_EDITION=local                    # local 或 cloud

# === CSS 配置 ===
CSS_PORT=5737                         # Local 默认: 5737, Cloud 默认: 6300
# CSS_BASE_URL=https://my-pod.example.com  # 自定义域名 (独立式)
CSS_LOGGING_LEVEL=info

# === 数据层 ===
# Cloud: postgresql://... | Local: sqlite:./data/...
CSS_SPARQL_ENDPOINT=sqlite:./data/quadstore.sqlite
CSS_IDENTITY_DB_URL=sqlite:./data/identity.sqlite

# === 对象存储 (Cloud 必须, Local 可选) ===
# CSS_MINIO_ENDPOINT=
# CSS_MINIO_ACCESS_KEY=
# CSS_MINIO_SECRET_KEY=
# CSS_MINIO_BUCKET_NAME=

# === Redis (Cloud 推荐, Local 可选) ===
# CSS_REDIS_CLIENT=

# === Local 托管式 ===
# 配置 XPOD_NODE_TOKEN 后自动连接 Cloud 获取身份服务和 DDNS
# XPOD_NODE_TOKEN=xxx
# XPOD_CLOUD_API_ENDPOINT=https://pods.undefineds.co

# === 隧道配置 (可选) ===
# 方案1: 有公网 IP / UPnP - 无需配置
# 方案2: Cloudflare Tunnel
# CLOUDFLARE_TUNNEL_TOKEN=xxx
# 方案3: SakuraFRP
# SAKURA_TOKEN=xxx

# === Local 独立式 ===
# 不配置 XPOD_NODE_TOKEN，用户自己管理域名和 IdP
# CSS_BASE_URL=https://pod.alice.com
# CSS_OIDC_ISSUER=https://id.undefineds.co  # 可选，使用 Cloud IdP

# === Cloud 子域名能力 (Cloud 模式可选) ===
# XPOD_SUBDOMAIN_ENABLED=true
# XPOD_DDNS_DOMAIN=undefineds.xyz
# CLOUDFLARE_API_TOKEN=
# CLOUDFLARE_ACCOUNT_ID=
# TENCENT_DNS_SECRET_ID=
# TENCENT_DNS_SECRET_KEY=
# XPOD_EDGE_NODES_ENABLED=false

# === Default Agent 回退 (仅用户 Pod 未配置时生效) ===
DEFAULT_PROVIDER=openrouter
DEFAULT_MODEL=stepfun/step-3.5-flash:free
DEFAULT_API_KEY=
# DEFAULT_BASE_URL=https://openrouter.ai/api/v1
# Claude Code 可执行文件路径（可选，默认自动查找）
# CLAUDE_CODE_PATH=/usr/local/bin/claude
