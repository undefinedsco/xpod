# === Xpod General Configuration Template ===

# --- Runtime Control (Supervisor) ---
XPOD_MODE=local # local or server
XPOD_CSS_PORT=8000
XPOD_API_PORT=8001

# --- General ---
CSS_LOGGING_LEVEL=info

# --- Data Layer ---
# Cloud: postgresql://... | Local: sqlite:./data/...
CSS_SPARQL_ENDPOINT=sqlite:./data/quadstore.sqlite
CSS_IDENTITY_DB_URL=sqlite:./data/identity.sqlite

# --- Storage (Required for Cloud) ---
CSS_MINIO_ENDPOINT=
CSS_MINIO_ACCESS_KEY=
CSS_MINIO_SECRET_KEY=
CSS_MINIO_BUCKET_NAME=

# --- Redis (Optional for Local, Recommended for Cloud) ---
CSS_REDIS_CLIENT=

# --- Cluster Center (Cloud Only) ---
CSS_EDGE_NODES_ENABLED=false
CSS_CENTER_REGISTRATION_ENABLED=false
CSS_CLUSTER_INGRESS_DOMAIN=localhost

# --- Edge Agent (Local Only) ---
CSS_EDGE_AGENT_ENABLED=false
CSS_SIGNAL_ENDPOINT=
CSS_NODE_ID=
CSS_NODE_TOKEN=
