# 白名单模式：默认忽略所有
*

# 构建需要的文件
!package.json
!bun.lock
!yarn.lock
!tsconfig.json
!scripts/
!scripts/patch-jose.js
!src/
!ui/
!config/
!static/
!templates/
!types/

# Explicitly excluded (not needed in image)
.github/
